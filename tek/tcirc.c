/* tcirc.c */

#include "arccom.h"
#include "tcom.h"

#define amax1(a,b)	(((a) < (b)) ? (a) : (b))

/*
 tcirc - tek circle
 */
void
tcirc_(int *pradius)
{
	/* function never called therefore not tested */
	return;
}

